{
  "name": "monorepo-ex",
  "version": "0.1.0",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client-main": "yarn workspace client-main",
    "client-manage": "yarn workspace client-manage",
    "server": "yarn workspace server",
    "common-const": "yarn workspace @common/const",
    "common-types": "yarn workspace @common/types"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "prettier": "^2.6.2",
    "typescript": "^4.6.4"
  }
}
